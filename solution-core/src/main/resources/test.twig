	@Test
	@TestInfo(platform = "{{ PLATFORM }}", domain = "{{ DOMAIN }}", group = "{{ GROUP }}", solutionClass = "{{ CLASS }}", taskDescription = "{{ DESCRIPTION }}")
	public void {{ METHOD }}() {
		runTest();
	}

