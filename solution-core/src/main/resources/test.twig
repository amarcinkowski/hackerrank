	@Test
	@TestInfo(platform = "{{ PLATFORM }}", domain = "{{ DOMAIN }}", subdomain = "{{ GROUP }}", classname = "{{ CLASS }}", taskDescription = "{{ DESCRIPTION }}")
	public void {{ METHOD }}() {
		runTest();
	}

